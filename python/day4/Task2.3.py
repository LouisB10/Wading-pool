print(list(filter(lambda x: x > 10, [42, 3, 4, 7])))
# cela va renvoyer un boolean selon si chaque élément de la liste est inférieur à 10
# le filter va faire en sorte de renvoyer une liste contenant seulement les nombres qui ont renvoyé true
# pour 42 : true, 3,4,7 : false
